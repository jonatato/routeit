import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.routeit.app',
  appName: 'RouteIt',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
